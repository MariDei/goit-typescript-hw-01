let variable: string | number; // змінна, яка може містити або рядок, або число (union type)
let status: "enable" | "disable"; // змінна, яка може містити лише одне з двох можливих рядкових значень: 'enable' або 'disable' (literal type)
